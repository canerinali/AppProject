@model List<DAL.Dto.CityDto>
@{
    ViewBag.Title = "CityIndex";
    Layout = "~/Views/_LayoutPage.cshtml";
}

<div class="col-lg-12">
    <div class="panel panel-default">
        <div class="panel-heading">
            Biletler
            <a href="@Url.Action("NewCity", "Home")" class="btn btn-success pull-right" style=" margin-top: -8px;">Yeni Ekle</a>
        </div>
        <!-- /.panel-heading -->
        <div class="panel-body">
            <div class="table-responsive">
                <table class="table table-striped table-bordered table-hover">
                    <thead>
                        <tr>
                            <th>Şehir</th>
                            <th>Ayarlar</th>
                        

                        </tr>
                    </thead>
                    <tbody>
                        @if (Model != null)
                        {
                            foreach (var item in Model)
                            {
                                <tr>
                                    <td>@item.Name</td>
                                    <td>
                                        <a href="@Url.Action("NewCity",new{id=item.Id})" class="btn btn-success"> Düzenle</a>
                                        <a href="@Url.Action("DeleteCity",new{id=item.Id})" class="btn btn-danger"> Sil</a>
                                    </td>
                                </tr>
                            }
                        }


                    </tbody>
                </table>
            </div>
            <!-- /.table-responsive -->
        </div>
        <!-- /.panel-body -->
    </div>
    <!-- /.panel -->
</div>
